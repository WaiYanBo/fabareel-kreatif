---
// src/components/Navbar.astro
---

<header class="site-header">
  <div class="nav-container">
    <a href="/" class="brand-group">
      <img src="/images/logo.webp" alt="Fabareel Kreatif Logo" class="nav-logo" onerror="this.style.display='none';" />
      <span class="brand-text">FABAREEL<span class="accent">KREATIF</span></span>
    </a>
  
    <nav class="nav-links">
      <a href="/about">About Us</a>
      
      <div class="mega-wrapper">
        <a href="/photography" class="mega-trigger">Photography</a>
        
        <div class="mega-menu">
          <div class="mega-content">
            
            <div class="mega-grid">
              <a href="/photography" class="mega-item">
                <img src="/images/wedding.jpg" alt="Weddings" loading="lazy" onerror="this.style.display='none';" />
                <span>Weddings</span>
              </a>
              <a href="/photography" class="mega-item">
                <img src="/images/vip-anwar.jpg" alt="Corporate" loading="lazy" onerror="this.style.display='none';" />
                <span>Corporate</span>
              </a>
              <a href="/photography" class="mega-item">
                <img src="/images/africa-doc.jpg" alt="Documentary" loading="lazy" onerror="this.style.display='none';" />
                <span>Documentary</span>
              </a>
              <a href="/photography" class="mega-item">
                <img src="/images/studio-desk.jpg" alt="Broadcast" loading="lazy" onerror="this.style.display='none';" />
                <span>Broadcast</span>
              </a>
            </div>

            <div class="mega-sidebar">
              <h4 class="sidebar-title">DISCOVER</h4>
              <a href="/photography">View All Categories</a>
              <a href="/photography">Cultural & Ceremonial</a>
              <a href="/photography">Commercial & Ads</a>
              <a href="/photography">Live Concerts</a>
              <a href="/photography">Studio Portraiture</a>
            </div>

          </div>
        </div>
      </div>
      
      <a href="/videography">Videography</a>
      <a href="/clients">Clients</a>
      <a href="/#work">Our Work</a>
    </nav>
  
    <div class="nav-actions">
      <a href="/#contact" class="cta-button">Let's Talk</a>
      
      <button class="mobile-toggle" aria-label="Toggle Menu">
        <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
          <line x1="3" y1="12" x2="21" y2="12"></line>
          <line x1="3" y1="6" x2="21" y2="6"></line>
          <line x1="3" y1="18" x2="21" y2="18"></line>
        </svg>
      </button>
    </div>
  </div>
  
  <div class="mobile-menu">
    <a href="/about">About Us</a>
    <a href="/photography">Photography</a>
    <a href="/videography">Videography</a>
    <a href="/clients">Clients</a>
    <a href="/#work">Our Work</a>
    <a href="/#contact" class="mobile-cta">Let's Talk</a>
  </div>
</header>
  
<style>
  .site-header {
    position: fixed;
    top: 0;
    left: 0;
    width: 100%;
    z-index: 1000;
    background: rgba(10, 10, 10, 0.7);
    backdrop-filter: blur(12px);
    -webkit-backdrop-filter: blur(12px);
    border-bottom: 1px solid rgba(255, 255, 255, 0.05);
    transition: all 0.3s ease;
  }

  .nav-container {
    display: flex;
    justify-content: space-between;
    align-items: center;
    max-width: 1200px;
    margin: 0 auto;
    padding: 1rem 2rem;
  }

  .brand-group {
    display: flex;
    align-items: center;
    gap: 12px;
    text-decoration: none;
  }

  .nav-logo {
    height: 40px;
    width: auto;
    object-fit: contain;
    display: block;
    transition: transform 0.3s ease;
  }

  .brand-group:hover .nav-logo {
    transform: scale(1.05); 
  }

  .brand-text {
    font-size: 1.25rem;
    font-weight: 800;
    color: white;
    letter-spacing: 1px;
    line-height: 1; 
  }
  
  .brand-text .accent {
    color: var(--text-muted);
    font-weight: 400;
    margin-left: 4px;
  }

  .nav-links {
    display: flex;
    gap: 2rem; 
    align-items: center;
    height: 100%; 
  }

  .nav-links > a, .mega-trigger {
    color: #e5e7eb;
    text-decoration: none;
    font-size: 0.95rem;
    font-weight: 500;
    transition: color 0.3s ease;
    padding: 1.5rem 0; 
  }

  .nav-links > a:hover, .mega-trigger:hover {
    color: white;
  }

  .mega-wrapper {
    position: static; 
    height: 100%; 
    display: flex;
    align-items: center;
  }

  .mega-menu {
    position: absolute;
    top: 100%; 
    left: 0;
    width: 100vw;
    background: #0a0a0a; 
    border-bottom: 1px solid rgba(255, 255, 255, 0.1);
    opacity: 0;
    visibility: hidden;
    transform: translateY(-10px);
    transition: all 0.3s ease;
    pointer-events: none;
    box-shadow: 0 10px 30px rgba(0,0,0,0.5);
  }

  .mega-menu::before {
    content: '';
    position: absolute;
    top: -40px; 
    left: 0;
    width: 100%;
    height: 40px;
    background: transparent; 
  }

  .mega-wrapper:hover .mega-menu {
    opacity: 1;
    visibility: visible;
    transform: translateY(0);
    pointer-events: auto;
  }

  .mega-content {
    max-width: 1200px;
    margin: 0 auto;
    padding: 3rem 2rem;
    display: grid;
    grid-template-columns: 3fr 1fr;
    gap: 4rem;
  }

  .mega-grid {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: 2rem;
  }

  .mega-item {
    text-decoration: none;
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 1rem;
    transition: transform 0.2s ease;
  }

  .mega-item:hover {
    transform: translateY(-5px);
  }

  .mega-item img {
    width: 100%;
    aspect-ratio: 16/9;
    object-fit: cover;
    border-radius: 8px;
    border: 1px solid #334155;
    background-color: #1e293b; 
  }

  .mega-item span {
    color: white;
    font-size: 0.95rem;
    font-weight: 600;
  }

  .mega-sidebar {
    display: flex;
    flex-direction: column;
    gap: 1rem;
    border-left: 1px solid #334155;
    padding-left: 3rem;
  }

  .sidebar-title {
    color: var(--text-muted);
    font-size: 0.8rem;
    letter-spacing: 2px;
    margin-bottom: 0.5rem;
  }

  .mega-sidebar a {
    color: #e5e7eb;
    text-decoration: none;
    font-size: 1rem;
    transition: color 0.2s ease;
  }

  .mega-sidebar a:hover {
    color: white;
  }

  .nav-actions {
    display: flex;
    align-items: center;
    gap: 1rem;
  }

  .cta-button {
    background: white;
    color: black;
    padding: 0.6rem 1.5rem;
    border-radius: 4px;
    text-decoration: none;
    font-weight: 600;
    font-size: 0.9rem;
    transition: transform 0.2s ease, background 0.2s ease;
  }

  .cta-button:hover {
    background: #e5e7eb;
    transform: translateY(-2px);
  }

  .mobile-toggle {
    display: none;
    background: none;
    border: none;
    color: white;
    cursor: pointer;
    padding: 0.5rem;
  }

  .mobile-menu {
    display: none;
    flex-direction: column;
    background: #0a0a0a;
    padding: 1rem 2rem 2rem;
    border-top: 1px solid rgba(255, 255, 255, 0.05);
  }

  .mobile-menu a {
    color: white;
    text-decoration: none;
    padding: 1rem 0;
    border-bottom: 1px solid rgba(255, 255, 255, 0.05);
    font-size: 1.1rem;
  }

  .mobile-menu a.mobile-cta {
    color: black;
    background: white;
    text-align: center;
    border-radius: 4px;
    margin-top: 1rem;
    border: none;
  }

  @media (max-width: 950px) { 
    .nav-links { display: none; }
    .cta-button { display: none; }
    .mobile-toggle { display: block; }
    .mobile-menu.active { display: flex; }
    
    @media (max-width: 480px) {
      .brand-text { display: none; }
    }
  }
</style>
  
<script>
  const toggleBtn = document.querySelector('.mobile-toggle');
  const mobileMenu = document.querySelector('.mobile-menu');

  if (toggleBtn && mobileMenu) {
    toggleBtn.addEventListener('click', () => {
      mobileMenu.classList.toggle('active');
    });
  }
</script>